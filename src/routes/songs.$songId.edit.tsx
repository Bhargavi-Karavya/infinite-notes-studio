import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { AdminShell } from "@/components/admin/AdminShell";import { ConfirmModal } from "@/components/admin/ConfirmModal";import { SongForm } from "@/components/admin/SongForm";import { BackLink,PageHeader } from "@/components/admin/ui";import { getSong } from "@/lib/songs";
export const Route=createFileRoute("/songs/$songId/edit")({head:()=>({meta:[{title:"Edit Song — Infinite Notes Admin"},{name:"description",content:"Edit song metadata, artwork, and lyrics."},{property:"og:title",content:"Edit Song — Infinite Notes Admin"},{property:"og:description",content:"Update an Infinite Notes catalog record."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Edit});function Edit(){const {songId}=Route.useParams();const song=getSong(songId);const [deleting,setDeleting]=useState(false);return <AdminShell><BackLink to="/songs" label="Songs"/><PageHeader eyebrow="Catalog editor" title={`Edit ${song.title}`} description={`Update ${song.artist}’s song metadata, lyrics, and artwork.`}/><SongForm song={song} onDelete={()=>setDeleting(true)}/>{deleting&&<ConfirmModal title={song.title} onClose={()=>setDeleting(false)} onConfirm={()=>history.back()}/>}</AdminShell>}
