import { AlertTriangle, Trash2, X } from "lucide-react";
import { Button } from "./ui";
export function ConfirmModal({title,onClose,onConfirm}:{title:string;onClose:()=>void;onConfirm:()=>void}) {return <div className="modal-layer" role="presentation"><button className="modal-scrim" onClick={onClose} aria-label="Close dialog"/><div className="modal" role="alertdialog" aria-modal="true" aria-labelledby="delete-title"><div className="modal-head"><div className="danger-icon"><Trash2/></div><div><h2 id="delete-title">Delete Song Record</h2><p>Permanent destructive administrative action</p></div><button className="icon-button" onClick={onClose} aria-label="Close"><X/></button></div><div className="modal-body"><div className="delete-target"><strong>{title}</strong><small>Catalog song and all associated metadata</small></div><div className="warning-box"><AlertTriangle/><div><strong>Warning: this cannot be undone</strong><p>The public song page, lyrics, and catalog links will be permanently removed.</p></div></div></div><div className="modal-foot"><Button variant="secondary" onClick={onClose}>Cancel & Keep Song</Button><Button variant="danger" onClick={onConfirm}><Trash2/> Confirm Deletion</Button></div></div></div>}
